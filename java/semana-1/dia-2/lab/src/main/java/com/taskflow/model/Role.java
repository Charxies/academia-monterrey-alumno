package com.taskflow.model;

/**
 * Role — rol de un usuario. Enum trivial y COMPLETO (aquí NO hay TODO).
 * Nota: el rol es un enum, NO una jerarquía de herencia — ver el cierre de la teoría T4.
 */
public enum Role {
    USER,
    ADMIN
}
