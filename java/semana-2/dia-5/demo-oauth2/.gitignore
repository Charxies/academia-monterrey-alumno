target/
# Secretos REALES de Google: NUNCA se commitean (solo la plantilla .example y los placeholders del yml).
src/main/resources/application-local.yml
*.log
.idea/
*.iml
# Eclipse (mismo criterio: config local del IDE, no fuente)
.project
.classpath
.settings/
bin/
