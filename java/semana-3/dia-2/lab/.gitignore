# TODO integrador (paso 1): añade aquí el .env con tus secretos 12-factor (S3D2) para que NUNCA se
# commitee. Solo viaja .env.example (plantilla sin valores reales). El DoD-(e) del día lo verifica.
#   Descomenta:
# .env

# Build
target/

# Base de datos H2 en ARCHIVO (runtime). La BD NO se commitea — contraste explícito con el
# tasks.csv de S1D5, que SÍ viajaba en el repo. Aquí los datos son estado local de dev, no fuente.
data/*.mv.db
data/*.trace.db

# IDE
.idea/
*.iml
.vscode/
